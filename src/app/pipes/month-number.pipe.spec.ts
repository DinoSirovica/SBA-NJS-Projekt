import {MonthNumberPipe} from './month-number.pipe';

describe('MonthNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
